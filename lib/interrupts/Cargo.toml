[package]
name = "interrupts"
version = "0.1.0"
authors = ["lsneff"]

[dependencies]
x86_64 = "0.1.2"
lazy_static = { version = "0.2.11", features = ["spin_no_std"] }

# internal crates
vga = { path = "../vga" }
memory = { path = "../memory" }